---
import { ReactionParty } from './reaction-party';

interface Props {
  contentType: 'blog' | 'pebbles';
  slug: string;
}

const { contentType, slug } = Astro.props;
---

<ReactionParty client:load contentType={contentType} slug={slug} />